<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zw.admin.server.dao.DeviceTypeDao">

	<sql id="where">
		<where>
			<if test="params.id != null and params.id != ''">
				and id = #{params.id} 
			</if>
			<if test="params.name != null and params.name != ''">
				and name = #{params.name} 
			</if>
			<if test="params.parent_id != null and params.parent_id != ''">
				and parent_id = #{params.parentId} 
			</if>
			<if test="params.create_time != null and params.create_time != ''">
				and create_time = #{params.createTime} 
			</if>
			<if test="params.update_time != null and params.update_time != ''">
				and update_time = #{params.updateTime} 
			</if>
		
		</where>
	</sql>

	<select id="count" resultType="int">
		select count(1) from device_type t
		<include refid="where" />
	</select>

	<select id="list" resultType="DeviceType">
		select * from device_type t
		<include refid="where" />
		order by id desc
		<if test="offset != null ">
			limit #{offset}, #{limit}
		</if>
	</select>

	<update id="update">
		update device_type t
		<set>
			<if test="name != null">
				name = #{name}, 
			</if>
			<if test="parent_id != null">
				parent_id = #{parentId}, 
			</if>
			<if test="create_time != null">
				create_time = #{createTime}, 
			</if>
			<if test="update_time != null">
				update_time = #{updateTime}, 
			</if>

		</set>

		where t.id = #{id}
	</update>

</mapper>
